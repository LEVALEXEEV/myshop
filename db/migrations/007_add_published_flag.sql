-- =============================================================
-- 007_add_published_flag.sql
-- Флаг публикации товара в каталоге.
-- По умолчанию товар скрыт; админ публикует вручную.
-- =============================================================

ALTER TABLE products
  ADD COLUMN IF NOT EXISTS published BOOLEAN NOT NULL DEFAULT FALSE;
